<div class="header">
   <h2><div class="title">Disney Vacation Planner</div></h2>
        <%if current_user %>
          <div class="username">
                <i>Welcome, <%= current_user.username %></i>
            </div>
        <% end %>
        <div class="nav">
        <div><a href="/attractions">Attractions</a></div>

    <% if logged_in? == true %>
        <div><a href="/vacation_plans">My Vacation Plans</a></div>
        <div><a href="/logout">Log Out</a></div>
    <% else%>
        <div><a href="/signup">Sign Up</a></div>
        <div><a href="/login">Log In</a></div>
    <% end %> 
 </div>
    