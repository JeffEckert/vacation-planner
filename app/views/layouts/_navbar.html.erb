<div class="header">
    <div class="title">Disney Vacation Planner</div>
        <%if current_user %>
          <div class="username">
                <i>Welcome, <%= current_user.username %></i>
            </div>
        <% end %>
        <div class="nav">
        <div><a href="/attractions">Attractions</a></div>

    <% if logged_in? == true %>
        <div><a href="/vacation_plans">My Vacation Plans</a></div>
        <div><a href="/logout">Log Out</a></div>
    <% else%>
        <div><a href="/signup">Log Out</a></div>
        <div><a href="/login">Log Out</a></div>
    <% end %> 
 </div>
    